export const POST_AUTH_SPLASH_TIMING = {
  // Главный параметр: суммарная длительность заставки
  TOTAL_DURATION_MS: 5000,
  // Когда начинать печать текста
  TYPE_START_DELAY_MS: 1800,
  // Пауза перед автозакрытием после завершения печати
  AFTER_TYPING_BUFFER_MS: 250,
  // Тайминги анимации солнца
  SUN_ANIMATION_MS: 2400,
  SUN_FILL_DELAY_MS: 800,
  // Плавное появление текстового блока
  TEXT_REVEAL_DELAY_MS: 1700,
  TEXT_REVEAL_DURATION_MS: 700,
  // Границы скорости печати (мс на символ)
  TYPE_INTERVAL_MIN_MS: 18,
  TYPE_INTERVAL_MAX_MS: 80,
};
